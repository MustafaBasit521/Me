import './Vignette.css'

// Darkens the screen edges so the eye reads as the one lit thing in the room.
function Vignette() {
  return <div className="vignette" aria-hidden="true" />
}

export default Vignette
